{
  "name": "nexus-desktop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "electron/main.mjs",
  "scripts": {
    "build:electron": "node scripts/build.mjs",
    "package": "cd ../client && vite build && cd ../desktop && npm run build:electron && electron-builder",
    "dev": "cd ../client && vite build && cd ../desktop && npm run build:electron && electron ."
  },
  "devDependencies": {
    "electron": "^39.8.10",
    "electron-builder": "^26.8.1",
    "esbuild": "^0.25.0"
  }
}
